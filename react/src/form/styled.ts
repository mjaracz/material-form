import {makeStyles} from '@material-ui/styles';

export const useStyle = makeStyles({
  root: {
    width: '30vw',
    height: 200,
    margin: '120px auto',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  }
})